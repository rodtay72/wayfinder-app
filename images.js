// ============================================
// WAY FINDER - IMAGE DATA
// ============================================
// Image data stored as base64-encoded SVG.
// You can replace these with actual image files later.

// Export for module systems
if (typeof module !== 'undefined' && module.exports) {
  module.exports = {  };
}
